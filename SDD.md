### 프로젝트 요약
python이랑 streamlit을 이용해서 AI가 영어 지문을 요약해주고, 중요 단어도 정리해주고, 퀴즈도 만들어주고, 내 progress까지 알려주는 AI Study Mate라는 이름의 프로그램을 만들어줘. 

### 사용할 기술(skill)
언어는 python, 프레임워크는 streamlit을 써줘. AI model은 google/flan-t5-small은 써줘.

### 주요기능
요약 기능
입력 받은 영어 지문을 읽고, 그 지문을 요약하는 기능이다. 요약 길이를 드롭다운을 이용해서 short, medium, detailed 중에서 선택할수 있다. 퀴즈의 개수도 선택할수 있다. 영어 지문은 40단어 이상이여야 한다. 40글자 미만이면, 경고 메시지를 띄우고 요약을 안 해준다. 이 모든 기능은 한 버튼을 누르면 이루어 지는데, 그 버튼의 이름은 Create study set이다.

주요 단어 추출 기능
입력 받은 영어 지문에서 많이 나온 단어 최대 8개를 추출한다. python logic으로만 단어를 추출해줘.

퀴즈 만들기 기능
python logic을 이용해서 입력 받은 영어 지문에서 문장을 랜덤으로 뽑아서 빈칸 채우기 식의 퀴즈를 선택 받은 만큼 만든다. 퀴즈 아래에는 답을 적고 enter키를 눌러서 제출 할 수 있는 input box를 만든다. Check answers라는 이름의 버튼을 누르면 채점 결과가 나온다. 전체 점수와 answer review가 나온다. 퀴즈를 다 풀고 채점 결과가 나오면, Create another quiz 라는 버튼이 나온다. 그 버튼을 누르면 새로운 퀴즈가 생성된다. 채점할 때는 정답이 대소문자 구분 하지 않는다.

진행 현황 측정 기능
완료한 퀴즈의 수, 최근 점수, 최고 점수를 보여줘. 점수는 percentage로 보여줘. x축이 퀴즐를 푼 회차, y축이 퀴즈의 정답률인 그래프를 그려줘. Clear progress이라는 버튼을 누르면 이전 완료한 퀴즈의 수, 최근 점수, 최고 점수의 데이터가 다 사라지게 해줘.


### UI 구성 (화면)
side bar를 만들어줘. 제목 아래에는 이 프로젝트에 대한 한 줄 요약하는 부제를 써줘. 그 아래에는 4개의 탭을 만들어줘. 그 탭들의 이름은 Study, Quiz, Progress, About으로 해줘. Study 탭에는 영어 지문 요약 기능이랑 주요 단어 추출 기능을 넣어줘. Quiz 탭에는 퀴즈 만들기 기능, Progress 탭에는 진행 현황 측벙 기능을 넣어줘. 그리고 About 탭에는 이 프로젝트에 대한 설명을 써줘.

### 부가적 내용들
side bar에는 이 프로그램에 대한 project information이랑 How it works를 설명해줘. project information에는 